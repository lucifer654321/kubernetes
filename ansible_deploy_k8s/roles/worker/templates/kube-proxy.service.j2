[Unit]
Description=Kubernetes Proxy
After=network.target

[Service]
EnvironmentFile={{remote_k8s_conf_dir}}/kube-proxy.conf
ExecStart={{remote_bin_dir}}/kube-proxy $KUBE_PROXY_OPTS
Restart=on-failure
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
