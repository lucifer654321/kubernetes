[defaults]
inventory = ./inventory
forks = 5
remote_user = root
ask_pass = False
host_key_checking = False
timeout = 10
log_path = /var/log/ansible.log

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false
